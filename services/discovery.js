/**
 * Executes a CMIS query statement against the contents of the repository.
 **/
exports.query = function (repositoryId, statement, searchAllVersions, includeRelationships, renditionFilter, includeAllowableActions, maxItems, skipCount) {
    //todo   
};

/**
 * Gets a list of content changes. This service is intended to be used by search crawlers or other applications that need to efficiently understand what has changed in the repository.
 **/
exports.getContentChanges = function (repositoryId, changeLogToken, includeProperties, includePolicyIds, includeACL, maxItems) {
    //todo   
};